CREATE TABLE TEST_SILVER.COMERCIAL.HISTORICO_VENTA_ZTND AS 
SELECT * FROM TEST_SILVER.COMERCIAL.SILVER_VENTA_NETA_CORP
WHERE ID_PAIS = 'DO'; 

SELECT * FROM STAGING_MANAGEMENT.INTEGRATIONS.MONITOREO_INGESTA_STAGING
WHERE NOMBRE_TABLA LIKE '%FABRIC%'
ORDER BY NOMBRE_TABLA DESC; 

SELECT * FROM TEST_SILVER.COMERCIAL.SILVER_VENTAS_SAP
where unidad_base <> 'ST'
AND LITROS_CALCULADOS_SNOW <> 0

; 

SELECT DISTINCT UNIDAD_BASE FROM TEST_SILVER.COMERCIAL.SILVER_VENTAS_SAP
where unidad_base <> 'ST'
AND LITROS_CALCULADOS_SNOW <> 0

SELECT 
NETWR,
CMPRE, 
WAVWR
FROM STAGING_RD_HT_GT.SAP.VBRP 
LIMIT 10; 

SELECT * FROM TEST_SILVER.COMERCIAL.SILVER_VENTA_NETA_CORP 
WHERE ID_PAIS = 'DO'
LIMIT 10
; 

SELECT * FROM TEST_SILVER.COMERCIAL.SILVER_VENTAS_SAP
where litros_calculados_snow <> litros_factura_calculado
order by 
; 

select * from staging_pe_ch.sql_fabric.venta
limit 10; 